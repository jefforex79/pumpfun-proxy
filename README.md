# pumpfun-proxy

A lightweight proxy for Pump.fun endpoints that:
- Adds standard headers
- Caches responses for 2–5 seconds
- Rate limits requests
- Exposes `/pumpfun/coins/advanced`

## Run locally
```bash
npm install
npm start
